end2end_eval:
  metrics:
    text_block:
      metric:
      - Edit_dist
    display_formula:
      metric:
      - Edit_dist
      # CDM 需要独立环境，默认关闭
    table:
      metric:
      - TEDS
      - Edit_dist
    reading_order:
      metric:
      - Edit_dist
  dataset:
    dataset_name: end2end_dataset
    ground_truth:
      data_path: /kaggle/working/results/benchmark/gt_subset.json
    prediction:
      data_path: /kaggle/working/results/benchmark/pred_md
    match_method: quick_match
